## so now the dlls are loaded correctly

SHLIB: lpsymphony

PKG_CPPFLAGS = -ISYMPHONY/include
PKG_LIBS = -llpsymphony -lm -L

CXX_STD = CXX14

R_symphony.o: lpsymphony

lpsymphony: 
		cp WinLib$(R_ARCH)/*.dll .
		

